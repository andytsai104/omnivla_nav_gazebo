# omnivla_bringup

Launch + config package for the project.

This package should:
- start Gazebo / robot / Nav2 related launch flow
- organize data collection mode
- organize inference + Nav2 mode
- organize evaluation mode
- hold shared YAML configs
